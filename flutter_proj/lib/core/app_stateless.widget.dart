import 'package:flutter/material.dart';

import 'package:flutter_proj/core/app.core.dart';

abstract class AppStatelessWidget extends StatelessWidget with AppCore {
  AppStatelessWidget({super.key});
}
